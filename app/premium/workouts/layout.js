export const metadata = {
  title: "Workout Library",
  description: "Browse our collection of premium yoga, pilates, and strength training workouts.",
};

export default function WorkoutsLayout({ children }) {
  return <>{children}</>;
}
